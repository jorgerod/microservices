spring:
  application:
    name: Config Server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/jorgerod/microservices.git
          basedir: config-server
server:
  port: 8888